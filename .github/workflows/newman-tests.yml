name: Run Newman Tests

on:
  push:
  pull_request:
  workflow_dispatch:  # i want to allows manual run
  schedule:
    - cron: '*/15 * * * *'  # i mean here i want to runs every 15 minutes

jobs:
  newman-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install Newman + Reporter
        run: |
          npm install -g newman newman-reporter-htmlextra

      - name: Run Postman Collection
        run: |
          newman run "RESTful Booker API.postman_collection.json" -r htmlextra

      - name: Upload Newman HTML Report
        uses: actions/upload-artifact@v4
        with:
          name: newman-report
          path: newman/
